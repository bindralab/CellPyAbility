* Make .csv cell viability matrix in GDA.py
* Hijack CP code for pipelines
* Use .csv inputs
* Loop the scripts so they could read several .csv files in a directory
* Standardize the naming format for wells